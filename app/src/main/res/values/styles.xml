<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="TextView_TypeMovie">
        <item name="android:textColor">@color/text_color_title</item>
        <item name="android:paddingTop">@dimen/text_padding_title</item>
        <item name="android:paddingBottom">@dimen/text_padding_title</item>
        <item name="android:textSize">@dimen/text_size_title</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_alignParentLeft">true</item>
        <item name="android:layout_alignParentTop">true</item>
    </style>
    <style name="TextView_ViewAll">
        <item name="android:textColor">@color/text_color_title</item>
        <item name="android:paddingTop">@dimen/text_padding_title</item>
        <item name="android:paddingBottom">@dimen/text_padding_title</item>
        <item name="android:textSize">@dimen/text_size_title</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:layout_alignParentTop">true</item>
        <item name="android:text">View All</item>
    </style>
</resources>
