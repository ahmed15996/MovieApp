<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:showIn="navigation_view">
    <group android:checkableBehavior="single">
    <item android:title="Moives"
            android:id="@+id/movies_navigation"
            android:iconTint="@android:color/darker_gray"
            android:icon="@mipmap/ic_movie"/>
        <item android:title="TV Show"
            android:id="@+id/tvShow_navigation"
            android:iconTint="@android:color/darker_gray"
            android:icon="@mipmap/ic_tv_show"/>
        <item android:title="Favorites"
            android:id="@+id/favorites_navigation"
            android:iconTint="@android:color/darker_gray"
            android:icon="@mipmap/ic_favorite"/>
        <item android:title="About"
            android:id="@+id/about_navigation"
            android:iconTint="@android:color/darker_gray"
            android:icon="@mipmap/ic_about"/>
    </group>


</menu>
